# Core Dependencies
fastapi>=0.115.0
uvicorn>=0.30.0
python-dotenv>=1.0.0
Werkzeug>=2.3.7
Jinja2>=3.1.2
click>=8.1.3
itsdangerous>=2.2.0
python-socketio>=5.11.0

# Face Recognition and Analysis
opencv-contrib-python>=4.8.1.78  # Required for cv2.face (LBPH)
numpy>=1.26.2
Pillow>=10.1.0
scikit-learn>=1.3.2  # For face similarity calculations
scipy>=1.11.3  # Required for scikit-learn
insightface>=0.7.3  # Required for face recognition
mediapipe>=0.10.0  # Required for facial landmarks and pose estimation

# WebRTC and Video Streaming
aiortc>=1.5.0
websockets>=12.0
requests>=2.31.0

# Database
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
SQLAlchemy>=2.0.23
pandas>=2.1.0  # Required for data export
openpyxl>=3.1.0  # Required for Excel export

# Security
cryptography>=41.0.5
PyJWT>=2.8.0
bcrypt>=4.0.1

# Monitoring and Logging
psutil>=5.9.6
loguru>=0.7.2  # Enhanced logging

# Production
gunicorn>=21.2.0

# Additional AI/ML Dependencies
onnxruntime>=1.15.1  # Required for InsightFace models

# Development and Testing
pytest>=7.4.2
pytest-cov>=4.1.0
httpx>=0.27.0
pytest-asyncio>=0.24.0

# Additional utilities
python-dateutil>=2.8.2
xlsxwriter>=3.1.0  # Required for Excel export

# Enhanced Anti-Spoofing System Dependencies

# Deep Learning Anti-Spoofing
torch>=1.9.0  # PyTorch for deep learning anti-spoofing model
torchvision>=0.10.0  # Required for image preprocessing transforms
gdown>=4.7.1  # For downloading pre-trained models from Google Drive

# Alternative: For GPU support (uncomment if you have CUDA and want GPU acceleration)
# torch>=1.9.0+cu111
# torchvision>=0.10.0+cu111

# Note: The anti-spoofing system will automatically detect if PyTorch is available
# If not available, it will fall back to traditional computer vision methods
# which are still effective but less accurate than the deep learning approach

# Additional Dependencies
onnxruntime>=1.15.0  # Required for insightface with GPU support on Mac M1/M2 via CoreML

# Optional Dependencies (uncomment if needed)
# redis>=5.0.1  # Only if using Redis for caching
# pgvector>=0.2.3  # Only if using vector similarity search in PostgreSQL
# Optional cache backend goes here if caching is added later
